[Unit]
Description=Set Tmux environment from SystemD
Requires=tmux@%i.service
After=tmux@%i.service

[Service]
Type=oneshot
ExecStart=/bin/sh -c '/usr/bin/systemctl --user show-environment | sed -e "s/=/ /g" | xargs -n2 /usr/bin/tmux set-environment -t %I'

[Install]
WantedBy=wm.target
